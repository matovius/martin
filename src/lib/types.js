/**
 * @typedef BlogPost
 * @prop { string } title
 * @prop { string } description
 * @prop { string } date
 * @prop { string } [updated]
 * @prop { string[] } categories
 * @prop { string } slug
 * @prop { boolean } published
 */
